// Replace these values with the buyer's Firebase web app config.
// Keep this file separate so the template can be sold without exposing secrets.
window.BUDGETAPP_FIREBASE_CONFIG = {
  apiKey: "YOUR_API_KEY",
  authDomain: "YOUR_PROJECT.firebaseapp.com",
  projectId: "YOUR_PROJECT_ID",
  storageBucket: "YOUR_PROJECT.appspot.com",
  messagingSenderId: "YOUR_SENDER_ID",
  appId: "YOUR_APP_ID"
};

window.BUDGETAPP_SETTINGS = {
  enableFirebase: false,
  requireWhitelist: false,
  sellerSupportEmail: "support@example.com"
};

